This displays the lists avaiable for view/edit for that user.
It is filtered by a users division code, so if a list and that user share the same division code then that user may view/edit the list.
The user can also create lists. 

sql for how its filtered in list_for_user.sql

